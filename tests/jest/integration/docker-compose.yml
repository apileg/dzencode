version: "3.9"

services:
    db:
        image: mysql:8.0.33
        restart: on-failure
        container_name: "integration-tests-mysql"
        ports:
            - "3307:3306"
        environment:
            MYSQL_ALLOW_EMPTY_PASSWORD: true
            MYSQL_DATABASE: tests
